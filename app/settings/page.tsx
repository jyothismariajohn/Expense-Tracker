export default function SettingsPage() {
    return (
        <div className="glass-panel" style={{ padding: '2rem' }}>
            <h1>Settings</h1>
            <p style={{ color: 'var(--color-text-muted)', marginTop: '1rem' }}>
                Application settings and supervisor management config will appear here.
            </p>
        </div>
    );
}
